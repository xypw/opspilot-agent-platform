# 9 月 26 日投递倒排计划（2026-09-15 更新）

目标：9 月 22 日冻结 OpsPilot 与 TracePilot 的可演示功能和证据，9 月 23–25 日完成三轮模拟面试并同步修订简历，9 月 25 日交付最终 PDF，9 月 26 日检查链接后开始投递。
按每天 6–8 小时计算，9 月 15–22 日用于项目与证据，9 月 23–25 日用于面试训练和简历定稿。功能范围以验收线为准，不为增加关键词扩展多 Agent、任意命令执行或没有评测支撑的功能。

## 当前起点

- OpsPilot 已有 FastAPI、LangGraph、多步工具、人工确认、Java 退货规则和写入、PostgreSQL/pgvector、Redis Checkpoint、Docker Compose。
- 9 月 13 日记录：隔离四服务 `mock + app` 7/7；真实模型 `live + isolated` 4/7，含两条 HTTP 429/1305。两者不能合并成“真实模型 7/7”。
- PDF 上传→pgvector 检索→Agent 引用→重启后仍可检索的真实联调已有记录。事实充分性核对器尚未接真实模型。
- `/demo` 的 HTML、CSS、JavaScript、FastAPI 路由与测试已写入；新测试尚未运行，页面未做浏览器验收，也未重新构建容器。
- README 和原始评测报告已有；一页简历已有可投递基础版，最终指标和第二项目尚未写入。当前未核实公开部署安全性。
- 现有 Agent Reliability Lab 只完成工具选择统计，保留为学习与评测代码，不再单独作为第二个简历主项目。
- 第二项目调整为 TracePilot：面向 Spring Boot 测试失败与异常堆栈的故障诊断和受控修复 Agent。

## 时间盒和验收线

| 截止日 | 交付物 | 验收标准 |
| --- | --- | --- |
| 9/15 | 范围冻结与教学准备 | 明确 OpsPilot 剩余证据和 TracePilot 最小闭环；固定两项目的业务问题、验收指标、代码边界和不做事项。继续训练 90 秒项目介绍。 |
| 9/16 | OpsPilot RAG 证据 | 扩充固定的虚构售后正反例，分开评测召回、证据充分性和引用支持；比较语义检索、混合检索及重排配置，保存数据集、配置、逐例结果和失败样本。 |
| 9/17 | OpsPilot Agent 证据 | 固定模型与任务集，测工具选择、任务终态、漏确认、重复写入和失败类型。真实模型或外部 Reranker 调用必须在用户明确批准后运行，并单独标注模型、环境和样本量。 |
| 9/18 | OpsPilot 冻结 | 复现知识问答和退货申请主链路，验收拒绝确认、重复确认、过期与恢复；补齐 README、架构图、演示脚本和原始报告，冻结简历可引用的证据。 |
| 9/19 | TracePilot 工具与故障集 | 建立独立项目和 Spring Boot 故障样例；实现受限的代码搜索、文件读取、Traceback 解析及指定 Maven 测试工具，禁止任意 Shell 和工作区外访问。 |
| 9/20 | TracePilot Agent 循环 | 用 LangGraph 编排“读取错误→收集上下文→提出假设→运行聚焦测试→形成诊断”，记录工具轨迹、上下文来源、失败原因和调用上限。 |
| 9/21 | TracePilot 确认与修复 | 生成可审查的补丁预览；用户确认后才允许修改白名单文件，随后重新运行聚焦测试。加入检查点、失败恢复、修改文件数和补丁大小限制。 |
| 9/22 | TracePilot 评测与冻结 | 使用固定故障集评测相关文件定位、修复后测试通过、平均工具次数和未经确认修改；保留逐例报告。README、启动命令、演示路径和测试通过后，才写入简历。 |
| 9/23 | 模拟面试第 1 天与简历候选版 | 模拟 Python/FastAPI、HTTP、RAG 检索、引用和评测；根据回答删改讲不清的表述，生成包含两项目真实证据的候选简历。 |
| 9/24 | 模拟面试第 2 天与项目复核 | 模拟 Tool Calling、LangGraph 状态、人工确认、Java 事务、幂等与恢复；现场阅读关键代码和排错，复核简历每个数字的报告入口。 |
| 9/25 | 模拟面试第 3 天与最终交付 | 按最终简历完整追问架构、贡献、失败案例和权衡；完成一页 Word/PDF、链接、文本复制顺序和仓库首页检查，当晚冻结最终可投递版。 |
| 9/26 | 投递检查与开始投递 | 只处理链接、联系方式或 PDF 显示等阻断问题，不再新增项目功能；开始投递 AI Agent 应用、LLM 应用后端及 Java+AI 集成岗位。 |

投递前版本保留一条能讲清的端到端业务链和真实故障故事。Word/OCR、泛化语义蕴含判断、复杂多 Agent、无鉴权公开部署，以及缺少标注支撑的“高准确率”宣传，移到投递后迭代。

TracePilot 只有同时具备独立 Spring Boot 故障集、受限工具调用、补丁确认、修复后测试、逐例评测报告和 README，才作为第二项目写入最终简历。现有 Reliability Lab 组件可复用为评测基础，但不能单独冒充完整项目。

教学方式：每个交付物先解释业务目标、代码入口和关键设计；学生参与一处实质性判断或编码；教师完成脚手架、重复代码和 Code Review。每天用“已验收/未验收”更新，不再用没有清单的百分比。
