# OpsPilot 的 AI Coding 工作流

这份记录说明开发者如何使用编码 Agent，而不是把“AI 辅助”写成一句无法验证的话。

## 工作循环

1. 把需求改写成可验证的行为和失败场景。
2. 只给编码 Agent 与当前问题有关的代码、契约和测试上下文。
3. 先确认业务不变量，再生成或修改代码。
4. 用针对性测试验证本次规则，再运行完整回归。
5. 阅读 Traceback，判断错误来自业务实现、接口契约、环境还是测试替身。
6. 把稳定且会重复使用的约定写入 `AGENTS.md`，把一次性任务留在当前提示中。

## 实例：退货确认与订单金额变化

原始需求是“用户确认后创建退货申请”。第一次实现虽然具备 `interrupt`，但审查时发现：
用户看到草稿金额399元，等待期间订单可能变成499元；如果直接执行，最终操作与授权内容不一致。

需求因此被展开为四个可测试规则：

- 确认前金额或商品变化，Java 返回 `409 ORDER_CHANGED`，不创建申请。
- LangGraph 保存 `STALE_CONFIRMATION`，刷新页面不能继续显示旧确认。
- 刷新产生新的 `draft_id`，旧 `approved=true` 不得继承。
- 申请已经创建后发生网络重试，返回原申请，不能重复创建。

实现时读取的上下文包括 LangGraph确认节点、Java草稿服务、Python HTTP网关、FastAPI异常映射和相关测试。
没有让模型读取 `.env`，也没有修改无关的RAG模块。

验证分为三层：Java单元测试验证规则和幂等；Python网关测试验证错误码契约；
FastAPI与真实LangGraph测试验证暂停、状态恢复、刷新和再次确认。Mock模式证明控制流程，
不证明真实模型的工具选择准确率。

一次测试曾出现 `Cannot reopen a client instance, once it has been closed`。从Traceback底部定位后，
确认生产网关每次请求创建新客户端，而测试重复使用已关闭的替身；最终只修改测试夹具，没有误改生产代码。

## 上下文、记忆和 Skill 的边界

- 当前任务提示：本次需求、临时假设和验收条件。
- `AGENTS.md`：跨任务稳定存在的架构边界、安全规则和测试命令。
- LangGraph Checkpoint：OpsPilot运行期间某个用户会话的业务流程状态。
- 数据库：业务事实与幂等记录，不能被聊天记忆代替。
- Skill：把已经稳定重复的执行流程封装为可调用能力。第一个项目级Skill是
  [opspilot-regression](.codex/skills/opspilot-regression/SKILL.md)，负责按改动边界选择测试并解释结果。

后续仍然只有当一个流程已经重复出现并且输入输出稳定时，才把它提取为Skill，
避免把普通说明文件换个名字。
